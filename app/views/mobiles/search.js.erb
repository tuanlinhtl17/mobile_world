<% if @mobiles.present? %>
  <% @mobiles[0..4].each_with_index do |mobile, index| %>
    $('<%= "#search-#{index} a" %>').attr('href', '<%= mobile_url mobile %>');
    $('<%= "#search-#{index} img" %>').attr('src', '<%= mobile.avatar %>');
    $('<%= "#search-#{index} span" %>').text('<%= mobile.name %>');
  <% end %>
<% else %>
  $('[data-toggle="popover"]').popover('hide');
<% end %>
